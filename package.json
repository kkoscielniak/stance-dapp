{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.12.2"
  },
  "scripts": {
    "once": "npx hardhat run scripts/run.ts",
    "local": "npx hardhat run --network localhost scripts/deploy.ts",
    "local-node": "npx hardhat node",
    "test": "npx hardhat test"
  }
}
